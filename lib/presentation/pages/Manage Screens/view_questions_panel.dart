import 'package:flutter/material.dart';

class ViewQuestionsPanel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'View Questions Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
